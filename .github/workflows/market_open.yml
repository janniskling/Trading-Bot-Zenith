name: Zenith – Market Open

on:
  schedule:
    - cron: '30 13 * * 1-5'  # 13:30 UTC = 15:30 Berlin (Mo-Fr)
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  market-open:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Market Open Step
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
          ALPACA_BASE_URL: ${{ secrets.ALPACA_BASE_URL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TRADING_START_DATE: ${{ secrets.TRADING_START_DATE }}
        run: python main.py --step market_open

      - name: Commit order log
        run: |
          git config user.name "Zenith Bot"
          git config user.email "zenith-bot@users.noreply.github.com"
          git add knowledge/daily_log.md
          git diff --staged --quiet || (git commit -m "chore: market-open orders $(date -u +%Y-%m-%d) [skip ci]" && git pull --rebase && git push)
